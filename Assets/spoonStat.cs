using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class spoonStat : MonoBehaviour
{
    public bool[] stats;
    public bool[] current;
    public GameObject[] objs;
    public int[] amounts;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
